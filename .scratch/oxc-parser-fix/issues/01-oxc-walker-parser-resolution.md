# 01 — Oxc-Walker Parser Resolution (`oxc-parser` & `rolldown` Bindings)

**What to build:** Align `oxc-parser` and `rolldown` native Windows binary bindings in `package.json` so `oxc-walker` resolves `parseSync` cleanly during `npx nuxt prepare`.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] Update `oxc-parser` in `package.json`
- [ ] Verify `oxc-walker` resolves `parseSync`
