# 02 — Package Lock & Postinstall Verification

**What to build:** Verify `npx nuxt prepare` executes without errors and generates `.nuxt` types cleanly across Node environments.

**Blocked by:** 01 — Oxc-Walker Parser Resolution (`oxc-parser` & `rolldown` Bindings)

**Status:** ready-for-agent

- [ ] Execute `npx nuxt prepare` cleanly
- [ ] Verify `◆ Types generated in .nuxt` output
