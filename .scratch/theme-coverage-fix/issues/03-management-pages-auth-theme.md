# 03 — Management Pages & Auth Flow Theme Synchronization

**What to build:** Refactor management module pages (`funds.vue`, `accounts.vue`, `items.vue`, `journal.vue`), configuration (`settings.vue`), and authentication screens (`login.vue`, `register.vue`) so inputs, tables, dropdowns, and tabs dynamically match the selected theme.

**Blocked by:** 01 — Global Theme CSS Token Architecture & App Shell Adaptation, 02 — Dashboard & Core Component Theme Synchronization

**Status:** completed

- [x] Refactor `journal.vue`, `funds.vue`, `accounts.vue`, `items.vue`
- [x] Refactor `settings.vue`
- [x] Refactor `login.vue` and `register.vue`
